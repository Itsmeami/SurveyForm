<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Responsive Form</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>

<body>
    <div class="h-screen w-screen bg-zinc-900 text-white flex justify-center items-center">
        <form action="/create" method="post" class="w-full max-w-md bg-zinc-700 p-6 rounded-lg shadow-lg">
            <h1 class="text-center text-3xl">Survey Form</h1>
            <!-- Name Input -->
            <label class="text-lg font-semibold">Name</label>
            <input type="text" name="name" placeholder="Enter your Name" required
                class="w-full rounded-lg p-2 mt-1 bg-zinc-600 text-white placeholder-gray-400 outline-none focus:ring-2 focus:ring-blue-400">

            <!-- Email Input -->
            <label class="text-lg font-semibold block mt-4">Email</label>
            <input type="email" name="email" placeholder="Enter your Email" required
                class="w-full rounded-lg p-2 mt-1 bg-zinc-600 text-white placeholder-gray-400 outline-none focus:ring-2 focus:ring-blue-400">

            <!-- Age Input -->
            <label class="text-lg font-semibold block mt-4">Age</label>
            <input type="number" name="age" placeholder="Enter Your Age" required min="1" max="120"
                class="w-full rounded-lg p-2 mt-1 bg-zinc-600 text-white placeholder-gray-400 outline-none focus:ring-2 focus:ring-blue-400">

            <!-- Select Dropdown -->
            <label class="text-lg font-semibold block mt-4">Which option describes you?</label>
            <select name="option"
                class="w-full rounded-lg p-2 mt-2 bg-zinc-600 text-white outline-none focus:ring-2 focus:ring-blue-400">
                <option selected disabled>Open</option>
                <option value="1">Good</option>
                <option value="2">Better</option>
                <option value="3">Best</option>
            </select>

            <!-- Submit Button -->
            <button type="submit"
                class="w-full bg-blue-500 text-white p-2 mt-5 rounded-lg shadow-md hover:bg-blue-600 transition">
                Submit
            </button>
        </form>
    </div>
</body>

</html>